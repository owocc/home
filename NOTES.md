# 项目说明与设计复盘

## 参考来源与工作模式

本项目没有可访问的原站 URL，也没有原站源码。输入仅包括用户提供的一张海报风格参考图，以及一张用户自绘人物形象。

采用的模式是“内容爆改 / 视觉语言提炼”：保留参考图中纸张拼贴、粗描边、硬阴影、撞色、超大排版和手写点缀的设计 DNA，再为个人简历、项目案例与博客重新组织内容和响应式页面。实现复杂度定义为 **L2**：多路由内容站，包含内容集合、静态详情生成、响应式视觉系统与轻量交互，但不包含后端、CMS 或重型图形渲染。

## 原创与边界

- 未复制参考图中的品牌、作者姓名、Adobe 图标、水印或具体文字排版。
- 参考图只用于理解视觉语气和构图节奏，不作为可直接复用的页面资产。
- 角色素材根据用户自绘人物的深色蓬松短发、暖棕肤色、大眼、红晕、耳机和黑衣等特征重新创作。
- `hero-peek.png`、`mascot-bust.png` 与 `paper-texture.webp` 均为本项目原创生成素材。
- 装饰性方格、标签、色块、字符贴纸与硬阴影由本站 CSS 原创建构。

## 技术栈与运行方式

- Astro 7.1.5，静态输出。
- Astro Content Layer、Markdown 与 Zod schema。
- TypeScript、Astro 组件、全局 CSS 和少量原生 JavaScript。
- 无 React、Tailwind、WebGL、粒子系统、自定义光标或外部字体依赖。
- `pnpm dev` 启动开发环境；`pnpm run build` 执行类型检查和静态构建。

## 原参考与本站的取舍

参考海报是单幅纵向视觉，本项目需要承载可浏览、可维护的多页内容，因此做了以下取舍：

- 保留米白纸张、方格、拼贴块面、黑色轮廓、橙黄蓝紫色板和夸张标题比例。
- 将单幅海报构图拆解为 Hero、卡片、章节标题、资料贴纸、正文边注与页尾等可复用组件。
- 英文大字用于视觉节奏，中文承担信息表达，避免牺牲内容可读性。
- 手工倾斜和不规则只用于非关键装饰；正文、导航、焦点状态和移动端顺序保持稳定。
- 动画仅用于一次性滚动显现、卡片反馈和桌面轻视差；系统启用减少动态效果时全部关闭。
- 触屏页面不依赖 hover，390px、768px 与桌面宽度均使用自然文档流避免横向溢出。

## 内容与素材替换地图

| 要替换的内容 | 唯一维护位置 | 影响范围 |
| --- | --- | --- |
| 姓名、角色、简介、所在地 | `src/data/site.ts` 的 `identity` | 页头、首页、简历、SEO 标题 |
| 经历、技能、教育 | `src/data/site.ts` | 首页简历摘要与完整简历页 |
| 邮箱和社交链接 | `src/data/site.ts` 的 `contact` | 首页联系区与简历联系区 |
| 项目 | `src/content/projects/*.md` | 首页精选、项目列表与详情页 |
| 博客文章 | `src/content/blog/*.md` | 首页最新文章、博客列表与详情页 |
| 首页角色 | `src/assets/images/hero-peek.png` | 首页 Hero |
| 简历角色 | `src/assets/images/mascot-bust.png` | 简历简介区 |
| 纸张纹理 | `src/assets/images/paper-texture.webp` | 全站背景 |
| 色彩、排版和响应式规则 | `src/styles/global.css` | 全站视觉系统 |

## 当前验证状态

- Astro 已升级至 7.1.5。
- `pnpm run build` 已通过，Astro check 为 0 errors、0 warnings、0 hints。
- 构建输出 13 个静态页面：首页、简历、项目列表、4 个项目详情、博客列表、4 篇文章详情与 `/404.html`。
- 项目与博客草稿均在列表查询和 `getStaticPaths()` 中过滤，草稿详情路由不会生成。
- 404 页面包含 `noindex, nofollow`。
- `git diff --check` 已通过。
- 网站仅保存在本地，未配置域名、托管、分析统计、CMS、搜索、评论或联系表单后端。
